import { defaultStyles } from '@/app/styles';
import { Text, View } from 'react-native';

const ArtistsScreen = () => {
  return (
    <View style={defaultStyles.container}>
      <Text style={defaultStyles.text}>Artist screen</Text>
    </View>
  );
};

export default ArtistsScreen;
